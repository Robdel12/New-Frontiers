---
import BaseLayout from '../layouts/BaseLayout.astro'
---

<BaseLayout
  title="404 - Page Not Found"
  description="The page you're looking for doesn't exist."
>
  <div class="error-page">
    <div class="racing-scene">
      <div class="car">🏎️</div>
    </div>
    <h1 class="error-title">Oops! Took a wrong turn</h1>
    <h2 class="error-code">404</h2>
    <p class="error-description">
      Looks like this page went off track! Even the best drivers sometimes miss
      their turn-in. Let me help you get back on course.
    </p>
    <div class="error-actions">
      <a href="/" class="error-button">🏁 Back to Start</a>
      <a href="/blog" class="error-button secondary">📝 Read Blog</a>
    </div>
    <p class="error-footer">
      <code>console.log("Where did this page go? 🤔")</code>
    </p>
  </div>

  <style>
    .error-page {
      text-align: center;
      padding: 80px 20px;
      max-width: 600px;
      margin: 0 auto;
    }

    .racing-scene {
      margin: 0 auto 32px;
    }

    .car {
      font-size: 4.5rem;
      filter: drop-shadow(0 6px 12px rgba(0, 0, 0, 0.15));
      display: inline-block;
      margin-bottom: 8px;
    }

    .error-title {
      font-size: 2.5rem;
      font-weight: 600;
      color: #111827;
      margin: 0 0 16px;
      line-height: 1.2;
    }

    .error-code {
      font-size: 7rem;
      font-weight: 800;
      color: #ffb70d;
      margin: 0 0 32px;
      line-height: 0.9;
      text-shadow: 0 8px 32px rgba(255, 183, 13, 0.25);
      font-family:
        system-ui,
        -apple-system,
        sans-serif;
      letter-spacing: -0.02em;
    }

    .error-description {
      font-size: 1.125rem;
      color: #374151;
      line-height: 1.6;
      margin-bottom: 48px;
      max-width: 480px;
      margin-left: auto;
      margin-right: auto;
    }

    .error-actions {
      display: flex;
      gap: 20px;
      justify-content: center;
      flex-wrap: wrap;
      margin-bottom: 48px;
    }

    .error-button {
      display: inline-block;
      padding: 16px 32px;
      background: #006eff;
      color: white;
      text-decoration: none;
      border-radius: 12px;
      font-weight: 600;
      transition: all 0.2s ease;
      font-size: 1rem;
      box-shadow: 0 4px 16px rgba(0, 110, 255, 0.2);
      border: 2px solid transparent;
    }

    .error-button:hover {
      background: #0056cc;
      transform: translateY(-1px);
      box-shadow: 0 8px 24px rgba(0, 110, 255, 0.25);
    }

    .error-button.secondary {
      background: transparent;
      color: #006eff;
      border: 2px solid #006eff;
      box-shadow: 0 4px 16px rgba(0, 110, 255, 0.08);
    }

    .error-button.secondary:hover {
      background: rgba(0, 110, 255, 0.05);
      transform: translateY(-1px);
      box-shadow: 0 8px 24px rgba(0, 110, 255, 0.15);
    }

    .error-footer {
      margin-top: 32px;
      opacity: 0.6;
      font-size: 0.875rem;
    }

    .error-footer code {
      background: rgba(0, 0, 0, 0.05);
      padding: 8px 16px;
      border-radius: 8px;
      font-family: 'SF Mono', Monaco, 'Consolas', monospace;
      color: #4b5563;
      font-size: 0.8rem;
      border: 1px solid rgba(0, 0, 0, 0.05);
    }

    :global(.dark) .error-title {
      color: #f9fafb;
    }

    :global(.dark) .error-description {
      color: #d1d5db;
    }

    :global(.dark) .error-button.secondary {
      color: #fbbf24;
      border-color: #fbbf24;
      box-shadow: 0 4px 16px rgba(251, 191, 36, 0.08);
    }

    :global(.dark) .error-button.secondary:hover {
      background: rgba(251, 191, 36, 0.1);
      color: #fde047;
      box-shadow: 0 8px 24px rgba(251, 191, 36, 0.15);
    }

    :global(.dark) .error-footer code {
      background: rgba(255, 255, 255, 0.08);
      color: #d1d5db;
      border-color: rgba(255, 255, 255, 0.05);
    }

    @media (max-width: 768px) {
      .error-page {
        padding: 60px 20px;
      }

      .car {
        font-size: 3.5rem;
      }

      .error-title {
        font-size: 2rem;
        margin-bottom: 12px;
      }

      .error-code {
        font-size: 5rem;
        margin-bottom: 24px;
      }

      .error-description {
        font-size: 1rem;
        margin-bottom: 40px;
      }

      .error-actions {
        flex-direction: column;
        align-items: center;
        gap: 16px;
        margin-bottom: 40px;
      }

      .error-button {
        width: 240px;
        padding: 14px 28px;
      }
    }
  </style>
</BaseLayout>
